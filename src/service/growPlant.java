package service;

import entity.Island;
import entity.Location;
import entity.Plant;

public class growPlant{

//    public void addPlant(Island plants){
//        plants.forEach((key, value) -> plants.compute(key, (k, v) -> v + 10));
//    }
}
